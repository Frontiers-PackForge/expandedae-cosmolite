package lu.kolja.expandedae.screen;

import java.util.HashMap;

import appeng.api.stacks.AEKey;
import appeng.client.gui.AEBaseScreen;
import appeng.client.gui.style.ScreenStyle;
import appeng.menu.AEBaseMenu;
import net.minecraft.network.chat.Component;
import net.minecraft.world.entity.player.Inventory;

public class ContainerOptimizePatterns extends AEBaseScreen {
    private var result;

    HashMap<AEKey, Integer> patterns = new HashMap<>();

    public ContainerOptimizePatterns(AEBaseMenu menu, Inventory playerInventory, Component title, ScreenStyle style) {
        super(menu, playerInventory, title, style);
        this.
    }

    public void setResult
}
